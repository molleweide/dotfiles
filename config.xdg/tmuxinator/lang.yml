# ~/.tmuxinator/vim.yml

#
# learn code project
#
# sets up a window for working with the my learn git cli project.
# haskell, rust, go, typescript
name: chat

windows:
  - tg:
      root: ~/code/tg
      panes:
        -
  - sg:
      root: ~/code/gurk-js
      panes:

  - imess:
      root: ~/code/imessageclient
      panes:
        -
