# https://docs.npmjs.com/misc/config
#
# Check config with:   npm config ls -l | grep / | grep -v '^; | sort'

# XDG Base Dirs compliance
cache=${XDG_CACHE_HOME}/npm
tmp=${XDG_RUNTIME_DIR}/npm
init-module=${XDG_CONFIG_HOME}/npm/config/npm-init.js

# https://stackoverflow.com/questions/34718528/nvm-is-not-compatible-with-the-npm-config-prefix-option
# Setting prefix is incompatible with nvm
# Use ~/bin/node-modules-dir "$@" to run a command with node prefix set
# prefix=${XDG_DATA_HOME}/npm

; # https://docs.npmjs.com/misc/config#link
; link





# username=jasonkarns
# email=jason.karns@gmail.com
#
# loglevel=warn
# scripts-prepend-node-path=auto
#
# cache=${XDG_CACHE_HOME}/npm
# devdir=${XDG_CACHE_HOME}/node-gyp
# ; prefix=${XDG_DATA_HOME}/npm
# ; tmp=${XDG_RUNTIME_DIR}/npm
#
# init-module=${XDG_CONFIG_HOME}/npm/init-module.js
# init-author-name=Jason Karns
# init-author-email=jason.karns@gmail.com
# init-author-url=http://jasonkarns.com
# init-version=0.0.0
# init-license=MIT
#
# @nodenv:registry=https://registry.npmjs.org/
# //registry.npmjs.org/:_authToken=${NPMJS_AUTHTOKEN}
#
# @github:registry=https://npm.pkg.github.com/
# //npm.pkg.github.com/:_authToken=${GHPR_AUTHTOKEN}
#
# # vi: ft=dosini
#
